# Backend — Lagos Fare API

Run from `backend/` with `PYTHONPATH=src` or install package in editable mode.

```bash
uvicorn lagos_fare.main:app --reload --app-dir src
```

See `docs/ARCHITECTURE.md` for layer boundaries.
